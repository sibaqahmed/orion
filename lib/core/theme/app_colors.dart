import 'package:flutter/material.dart';

class AppColors {
  // Backgrounds
  static const Color background = Color(0xFF0B0F1A); // deep charcoal
  static const Color surface = Color(0xFF12182A);

  // Primary (calm futuristic blue)
  static const Color primary = Color(0xFF6B7CFF);

  // Accent (VERY subtle – almost grey-blue)
  static const Color accent = Color(0xFF9FA8FF);

  // Text
  static const Color textPrimary = Color(0xFFEDEEFF);
  static const Color textSecondary = Color(0xFFB3B7D1);
}
